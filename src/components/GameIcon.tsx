import { View, Text } from 'react-native'
import React from 'react'
import Icon from 'react-native-vector-icons/Ionicons'

const GameIcon = () => {
  return (
    <Icon name="game-controller" size={38} color="#F7CD2E" />
  )
}

export default GameIcon